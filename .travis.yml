sudo: false
language: python

python:
  - 3.6

install:
  - pip install "Nikola[extras]" doctr

script:
  - set -e
  - cd docs
  - nikola build
  - doctr deploy . --deploy-branch-name gh-pages --built-docs output/
